Agenda : Python

- INTRODUCTION
- Advantages 
- Python IDE & Interpreter
- First python program(eg:hell world)
- Variables
- Built in functions(print,input,len...)
- Datatypes
        *string
        *numbers
        
- Operators
- Conditional construct
- Loops


- Exception handling

        * try....Except
        * else block

Introduction :

- Python is a programming language
- Create web application
- released in 1991,guido van rossum

uses:

* web development
* mathematics
* system scripting
* software development

Why ??

- more portable(differend OS - WINDOWS,MAC,Linux,raspberry pi etc)
- Open source
- simplex syntax
- Connect to database system (CRUD - Create,read,update,delete)
- Easily handle big data and perform complex calculations
- works with all SDLC models(rapid prototype)

Advantages :
 - Code becomes more readable (similar to engligh language)
 - use a proper closure to complete the lines
 - Relies on indentation

comments:
- explains the Code
- ignorned for execution

""" - multiline comments
# - single line comments

Variable :

- container to store values
- It is case sensitive 
- Quotes "" and '' are the same in python for denoting the string

eg :
 x= 10;

Assigning multiple values :

Global variables:

- variable created globally or outside the functions

def func():


Data types:

- Text type : str
- Number type : int,float,complex(10+2j)
- Sequence types : list,tuple,range
- Mapping type: dict
- Set Types : set, frozen set
- Boolean type :Boolean
- Binary : bytes,bytearray,memoryview
- None : noneType

Exception handling :

- handle errors at run time
try : set of statements that needs to be checked for errors
except : handle the errors
else : execute when there is no error ---optional
finally : execute irrespective of whether you have an error or not 

